# TODO

## Initial Java Straight Conversion

- Port upstream image asset collection and drawing package generation.
- Port upstream rich text, inline style, and hyperlink handling.
- Port upstream merge marker handling for `[←M←]` and `[↑M↑]`.
- Port upstream column hints and xlsx2md fixture compatibility behavior.
- Add parity tests against selected `miku-md2xlsx` fixtures.
- Decide whether Java-side Maven plugin support remains out of scope or moves
  to a separated `miku-md2xlsx-java-maven` repository.

